import {
    Component,
    PieChart,
    SankeyChart
} from 'index';

describe('index', () => {
    it('Component should be a Function', () => {
        // expect(Component).toBeInstanceOf(Function);
    });


    it('PieChart should be an Function', () => {
        // expect(PieChart).toBeInstanceOf(Function);
    });

    it('SankeyChart should be an Function', () => {
        // expect(SankeyChart).toBeInstanceOf(Function);
    });
});